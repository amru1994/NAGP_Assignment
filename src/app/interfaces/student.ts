export interface IStudent {
    id?: number,
    studentName?: string,
    category?: string,
    dob?: Date,
    fatherName?: string,
    motherName?: string,
    lastClassScore?: number,
    domicile?: boolean,
    birthCertificate?: boolean,
    marksheets?: boolean,
    policeClearance?: boolean,
    passport?: true,
    declaration?: boolean
}